<li>
	{{name}}

	<button class="change-devour" data-id="{{id}}" data-newsleep="{{devour}}">
		{{#if devour}}Devoured!{{else}}Eat!{{/if}}
	</button>

{{!-- <button class="delete-cat" data-id="{{this.id}}">DELETE!</button> --}}
</li>